"use strict";

/**
 *
 */

apsgUI.controller('adminCtrl', function ($scope, $log){


    /**
     * Group all wanted actions for the controller initialisation
     */
    function init() {
        $log.debug("adminCtrl:init()");
    }

    /**
     * Start the magic
     */
    init();
});
