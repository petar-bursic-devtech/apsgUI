"use strict";

/**
 * Handle login page functionality and/or permissions (WUUUT)
 */

apsgUI.controller('loginCtrl', function ($scope, $log){

    /**
     * Group all wanted actions for the controller initialisation
     */
    function init() {
        $log.debug("loginCtrl:init()");
    }

    /**
     * Start the magic
     */
    init();
});